<LinkTo
    @route={{@route}}
    @models={{if @model (array @model) this.models}}
    @activeClass={{this.activeClass}}
    @query={{if @query @query (hash)}}
    @replace={{@replace}}
    @current-when={{@currentWhen}}
    class="dropdown-link dropdown-item"
    {{on "click" this.onClick}}
    ...attributes
>
    {{yield}}
</LinkTo>